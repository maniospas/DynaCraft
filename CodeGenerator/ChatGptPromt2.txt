float base_salary = 50000.0;

def employee(int id, float salary){
    object e = object();
    float e.id = id;
    float e.salary = salary;
    
    ! return e;
}

def manager(int id, float salary, float bonus){
    employee m = employee(id, salary);
    float m.bonus = bonus;
    
    ! return m;
}

def developer(int id, float salary, int projects){
    employee d = employee(id, salary);
    int d.projects = projects;
    
    ! return d;
}

manager john = manager(1, base_salary, 15000.0);
developer jane = developer(2, base_salary + 20000.0, 5);

#print(john.bonus);   // result = 15000.0
#print(jane.projects); // result = 5

