float x = 5.3;
float y = 8.7;

def calculate(float p, float q) {
    object result = object();
    float intermediate = p * q + 3.6;
    ! return result;
}

while x < y: {
    object iteration = object();
    float diff = y - x;
    x = x + 0.5;
    ! return iteration;
}

if x == y: {
    object equal = object();
    float final_value = x + y * 2.2;
    ! return equal;
} else {
    object not_equal = object();
    float final_value = y - x * 1.8;
    ! return not_equal;
}